collections:
  days:
    output: true
    permalink: :name

defaults:

  - scope:
      path: ""
      type: "post"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag